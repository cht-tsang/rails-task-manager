<h1><%= "#{@task.title}" %></h1>

<p><%= "#{@task.details}" %></p>

<p><%= @task.completed == true ? "Task has been completed" : "Task is not yet completed" %>

<p><%= link_to "UPDATE TASK", edit_path %></p>

<p><%= link_to "Back To Task List", tasks_path %></p>